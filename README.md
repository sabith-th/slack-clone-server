# Slack Clone Backend Server

## Getting Started

Make sure you have node and npm installed. Also ensure that a postgres server is running.

Install all dependencies: `npm install`

Start the server: `npm start`
